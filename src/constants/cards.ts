const maxCardWidth = 0.85; // 'times' parent width
const maxCardHeight = 0.7; // 'times' parent height
const cardRatio = { x: 5, y: 8 }; // x/y ratio

export { maxCardWidth, maxCardHeight, cardRatio };
